---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: go-example-db
    release: primary
  name: go-example-db
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
